{
  "name": "datatochain-subgraph",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --product hosted-service",
    "deploy": "graph deploy --product hosted-service username/datatochain-sepolia",
    "deploy-studio": "graph deploy --studio datatochain-sepolia",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "DataToChain Subgraph for Sepolia Network",
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.30.0"
  },
  "dependencies": {
    "global": "^4.4.0"
  }
}
